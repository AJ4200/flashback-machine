# Sega ROMs

Add Sega Master System, Game Gear, Genesis, Mega Drive, or Sega CD files here, then add each filename to `segalist.json`.

Supported extensions: `.md`, `.gen`, `.smd`, `.bin`, `.sms`, `.gg`, `.sg`, `.cue`, `.iso`, `.chd`.
